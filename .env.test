# Used by `npm run test:e2e`. Points at a throwaway database that the suite
# truncates and reseeds on every run — never point this at your dev database.
DATABASE_URL="postgresql://realestate:realestate@localhost:5460/realestate_test?schema=public"
AUTH_SECRET="test-only-secret-uJ8kQ2mNvR7xT4pL9wZ3cB6yF1hG5sA0"
AUTH_SESSION_DAYS="7"
STORAGE_DRIVER="local"
LOCAL_UPLOAD_DIR="./storage/test-uploads"
NEXT_PUBLIC_SITE_URL="http://localhost:3399"
